# PROGRESS.md — 진행 상황 (세션 인수인계용)

> 사람용 요약. 머신 상태는 `workflow/state.yaml`(우선). 새 세션은 `npm run workflow:resume` 먼저.

## 현재
- Stage 05(구현) 완료 — **핵심 데모 라이브 동작·검증됨.**
- 제품 "첫걸음": 받은 행정문서(지급명령 등)/상황 → AI가 [뜻·기한·첫걸음·관할·출처] 카드. 실제 처리는 정부24로 핸드오프.
- 엔진 AI only(멀티모달+LLM), 출처는 검증된 fixture(`web/lib/sources.ts`)에서만 인용(환각 URL 차단, 서버 byId 필터).

## 완료
- 00 intake · 01 리서치(5트랙) · 02 인사이트/승인(concept.md) · 03 spec · 04 plan · 05 빌드(T1~T3 핵심).
- 빌드 게이트 PASS(빌드+타입+정체성). 라이브 검증: 지급명령 입력 → guide-card 4칸 + 출처배지 3개(easylaw·scourt 화이트리스트 URL), 관할="지급명령을 내린 법원"(거리 아님).
- 교차검토(각 단계 클로드 리뷰어): 모두 PASS/PASS-with-warnings, 지적 반영(출처코퍼스·matchSources 등).

## 다음
- Stage 06 통합(배포 URL·PWA 마감) → 07 데모검증(Playwright) → 08~10 발표 → 11 리허설/승인 → 12 패키지.
- 스트레치(시간 남으면): 사진 OCR(멀티모달 비전, 코드 준비됨), 전입신고 2번째 시나리오(데이터 준비됨), 관할 지도.

## 막힘 / 주의
- AI only = 라이브에서 키/네트워크 죽으면 데모 정지(코드 폴백 없음). 검증 샘플 + 시연 녹화 보험 필요.
- codex 교차검토 미작동 → 클로드 리뷰어 폴백으로 대체.
- 콘솔 `stroke-width` 경고는 키트 Header SVG(비치명적).
- 아직 git 체크포인트 커밋 안 함(사용자 요청 시).
